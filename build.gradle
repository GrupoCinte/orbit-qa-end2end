plugins {
    id 'java'
    id 'io.qameta.allure' version '2.12.0'
    id 'org.gradle.test-retry' version '1.5.8'
}

group = 'org.example'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    // Selenium
    implementation 'org.seleniumhq.selenium:selenium-java:4.16.1'

    // Cucumber (Core + Java)
    implementation 'io.cucumber:cucumber-java:7.15.0'

    // Motor para conectar Cucumber con JUnit 5
    testImplementation 'io.cucumber:cucumber-junit-platform-engine:7.15.0'

    // JUnit 5 (Plataforma y Suite para correr los tests)
    testImplementation 'org.junit.platform:junit-platform-suite:1.10.1'
    testImplementation 'org.junit.jupiter:junit-jupiter:5.10.1'

    implementation platform('io.qameta.allure:allure-bom:2.24.0')
    implementation 'io.qameta.allure:allure-cucumber7-jvm'
}

test {
    // IMPORTANTE: Activa JUnit Platform
    useJUnitPlatform()

    // Configuración para ver logs claros en consola
    systemProperty("cucumber.junit-platform.naming-strategy", "long")
    testLogging.showStandardStreams = true

    retry {
        // Si falla, reintenta hasta 2 veces más
        maxRetries = 2

        // Si hay más de 10 fallos distintos, deja de reintentar (algo grave pasa)
        maxFailures = 10

        // Si pasa en el reintento, marca la prueba como EXITOSA (no falla el build)
        failOnPassedAfterRetry = false
    }
}

allure {
    version = '2.24.0'
}